#include "main.h"
/**
 *print_alphabet- prints the alpha in lowercase
 *Return: return 0
 */
void print_alphabet(void)
{
char la;
for (la = 'a'; la <= 'z'; la++)
int _putchar(la);
int _putchar('\n');
}
