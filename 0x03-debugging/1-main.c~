#include <stdio.h>
/**
 *main- Entry point
 *Return: return 0
 */
int main(void)
{
int i;
printf("Infinite loop incoming :(\n");
i = 0;
  /**
   *while (i < 10)
   *{
   *putchar(i);
   *} 
   */
printf("Infintie loop avoided! \\o/\n");
return (0);
}
